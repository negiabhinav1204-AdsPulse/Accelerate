apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accelerate-agentic-service.fullname" . }}-test-connection"
  labels:
    {{- include "accelerate-agentic-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "accelerate-agentic-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
