"""Common domain — shared tools and blocks usable by any agent.

This is not an agent — it's a tool library. Other agents import from here.

Usage:
    from src.agentic_platform.app.common.tools.image_gen import generate_image
"""
